---
description: Update the Harness knowledge base and repo-config
---

1. Edit `harness/givesource.md` to add or update:
   - New subsystem pointers
   - Useful search queries
   - Invariants and runbooks

2. Edit `harness/repo-config.yml` when packages, commands, or runbooks change.

3. Commit the updates:
   ```bash
   git add harness/givesource.md harness/repo-config.yml && git commit -m "harness: update KB and repo-config"
   ```
