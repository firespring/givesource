a:link,
a:visited {
  color: {{color}};
}
a:hover,
a:active {
  color: #000;
}

.btn--accent,
.btn--accent:link,
.btn--accent:visited {
  background-color: {{color}};
  color: #fff;
}
.btn--accent:hover,
.btn--accent:active {
   background-color: #000;
}