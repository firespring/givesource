# Checkpoints (Append-only)

Short, time-stamped progress snapshots that link back to a plan. Keep each entry minimal and actionable.

- Filename format: `YYYYMMDD-HHMM-<topic>.md` (UTC timestamp).
- Include: plan link, what changed, open risks, next actions.
- Do not edit after creation; add a new checkpoint instead.
