schema_version: 1
min_plugin_version: "0.9.0"

repo:
  name: givesource
  prefix: gs
  base_branch: develop
  feature_branch_format: "<STORY_KEY>"

harness:
  main_file: harness/givesource.md
  amendments_dir: harness/amendments/
  checkpoints_dir: harness/checkpoints/

commands:
  test: npm run test
  lint: npm run lint

jira:
  session_labels: true
  closure: full

domain_skills: {}

context: {}

workflow_notes: {}
